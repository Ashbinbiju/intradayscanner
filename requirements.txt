# Streamlit Cloud installs this file by default, so it covers both the bot
# and the scanner UI. For a headless bot-only install the last three are
# unnecessary but harmless.

# --- bot ---
smartapi-python>=1.5.5
pyotp>=2.9.0
requests>=2.31.0
websocket-client>=1.7.0
logzero>=1.7.0

# --- scanner UI ---
streamlit>=1.40
pandas>=2.0
altair>=5.0
